networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    volumes:
      - next-app-volume:/app  # Монтируем volume
      - ./nginx.conf:/etc/nginx/nginx.conf    # Примонтировать nginx конфиг
      - ./frontend/.next:/frontend/_next      # Примонтировать файлы .next
      - ./frontend/public:/frontend/public   # Примонтировать файлы public
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
      - frontend
    networks:
      - dev

  postgres:
    image: postgres:16
    env_file:
      - ./backend/.development.env
    environment:
      PG_DATA: /var/lib/postgresql/data
    ports:
      - 5433:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - dev

  backend:
    build: 
      context: ./backend
    depends_on:
      - postgres
    networks:
      - dev
      
  frontend:
    build: 
      context: ./frontend
    volumes:
      - next-app-volume:/app  # Монтируем volume
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - dev


volumes:
  pgdata:
  
  